// Define the divide function
function divide() {
    return 2000 / 100;
  }
  
// Define the square arrow function
const square = (x) => {
    return x * x;
  }
  
// Define the add arrow function
const add = (a, b) => {
    return a + b;
  }
  
